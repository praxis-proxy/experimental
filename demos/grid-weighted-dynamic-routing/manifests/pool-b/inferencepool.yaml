apiVersion: inference.networking.k8s.io/v1
kind: InferencePool
metadata:
  name: "pool-b"
  namespace: grid-system
spec:
  targetPorts:
    - number: 8000
  selector:
    matchLabels:
      app: vllm-vcr
      pool: "pool-b"
  endpointPickerRef:
    name: llmd-epp
    port:
      number: 9002
