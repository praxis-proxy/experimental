# Presentation assets

The reusable introductory and closing slides live in the tracing repository:

```text
../praxis-tracing/grid-dynamic-weighting/slides/weighted-routing-deck.md
```

Keep presentation source in the tracing repository so UI, slide, subtitle, and
recording changes stay in one reviewable history. This demo repository owns
the deployable topology and qualification workflow.
